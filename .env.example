# UI kit — no production secrets required
NEXT_PUBLIC_UI_MOCK_LOGGED_IN=true
NEXT_PUBLIC_APP_URL=http://localhost:3002

# Placeholder values so shared lib code that reads env does not crash at build time
NEXT_PUBLIC_INSFORGE_URL=https://example.insforge.app
NEXT_PUBLIC_INSFORGE_ANON_KEY=ui-mock-anon-key
